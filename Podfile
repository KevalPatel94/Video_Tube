# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Video_Tube' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Video_Tube
  pod 'ObjectMapper', '~> 3.4'
  pod 'SDWebImage'
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'YouTubePlayer'

end
